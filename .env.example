DB_HOST=
DB_PORT=
DB_DATABASE=
DB_USERNAME=
DB_PASSWORD=

TINY_API_TOKEN=

NODE_ENV=
PORT=

GEMINI_API_KEY=

# Fallback por paginação
FALLBACK_MAX_PAGES_PER_RUN=    # quantas páginas processar em cada ciclo do tiny_sync
FALLBACK_PAGE_DELAY_MS=       # intervalo entre páginas (ms)
FALLBACK_RATE_LIMIT_SLEEP_MS= # quanto dormir quando a API bloquear (ms) — 60s
FALLBACK_JITTER_MS=         # jitter aleatório adicional (ms)

# Intervalo do cron (compose)
SYNC_INTERVAL_SEC=           # 15 min entre ciclos do tiny_sync